<%- include('../../shared/includes/head', {pageTitle : product.title }) %>
<link rel="stylesheet" href="/styles/products.css">
<!-- <link rel="stylesheet" href="/styles/base.css"> -->
<script src="/scripts/cart-management.js" defer></script>


</head>

<body>
  <%- include('../../shared/includes/header.ejs') %>
  <main id="product-details">
    <header>
      <img src="<%= product.imageUrl %>" alt="<%= product.title %>">
      <div id="product-info">
        <h1><%= product.title %></h1>
        <p>₹ <%= product.price %></p>
        <button class="btn" data-productid="<%= product.id %>" data-csrf="<%= locals.csrfToken %>">Add to Cart</button>
        <p>csrf :<%= locals.csrfToken %></p>
      </div>
    </header>

    <p><%= product.description %></p>
  </main>
  <%- include('../../shared/includes/footer.ejs') %>

  <!-- why suddenly only ids instead of class ? bc id is usefull in fronten js -->

  <!-- product.imageUrl is used since product, passed by controller to this view, is a Product class, which has product.imageUrl  field-->